import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // PWA will be added via service worker approach
  // next-pwa is not needed for basic PWA support with Next.js 15
}

export default nextConfig
